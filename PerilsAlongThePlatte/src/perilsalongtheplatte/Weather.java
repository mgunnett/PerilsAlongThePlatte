package perilsalongtheplatte;

/**
 * An enum to store the names of each weather state, since the name of each weather is used through various classes. 
 */
public enum Weather {
	SUNNY, 
	RAINY, 
	SNOWY, 
	WINDY, 
	THUNDERSTORMS;
}
